import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import World from './World';
import registerServiceWorker from './registerServiceWorker';

const worldWidth = 70  // cells
const worldHeight = 50; // cells


ReactDOM.render(<World width={worldWidth} height={worldHeight}/>, document.getElementById('root'));

registerServiceWorker();
